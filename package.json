{
  "name": "tree-clipper",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "wrangler": "^4.59.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1"
  }
}
